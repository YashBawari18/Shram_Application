/**
 * Backward compatibility wrapper redirecting to the new Premium Design System tokens
 */
import { Colors as DesignColors, Typography as DesignTypography, Spacing as DesignSpacing, Radius as DesignRadius, Shadow as DesignShadow } from '../design/theme';

export const Colors = DesignColors;
export const Typography = DesignTypography;
export const Spacing = DesignSpacing;
export const Radius = DesignRadius;
export const Shadow = DesignShadow;
